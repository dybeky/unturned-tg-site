# COBRA PEI #1 Website

Современный сайт для COBRA PEI #1 с custosAC античит системой.

## ✨ Возможности

### 🎨 Визуальные эффекты
- **Анимированный фон с частицами** - интерактивные частицы, реагирующие на движение мыши
- **Параллакс эффект** - плавное движение элементов при скролле и движении мыши
- **Плавные анимации** - fade-in, slide-up эффекты
- **Адаптивный дизайн** - отлично работает на всех устройствах

### 🌓 Темная/Светлая тема
- Переключатель темы в правом верхнем углу
- Плавный переход между темами
- Сохранение выбранной темы в localStorage
- Автоматическая адаптация всех элементов

### 📊 Функциональность
- **Счетчик посещений** - отслеживание уникальных посетителей (localStorage)
- **Live status сервера** - реальный статус с BattleMetrics API (онлайн/оффлайн, количество игроков)

## 🚀 Использование

### Открыть сайт
Просто откройте `index.html` в браузере.

### Настройка Live Status сервера

Сайт использует **BattleMetrics API** для получения реального статуса сервера.

**Текущий сервер:** COBRA ВЫЖИВАНИЕ #1 [PEI] (ID: 34747819)

#### Как изменить сервер:

1. Найдите свой сервер на [BattleMetrics](https://www.battlemetrics.com/servers/unturned)
2. Скопируйте ID сервера из URL (например, `/servers/unturned/34747818`)
3. Откройте `js/server-status.js`
4. Замените значение `serverId` в `SERVER_CONFIG`:

```javascript
const SERVER_CONFIG = {
    serverId: 'ВАШ_ID_СЕРВЕРА', // Замените на ID вашего сервера
    apiUrl: 'https://api.battlemetrics.com/servers/',
    checkInterval: 30000,
    timeout: 10000
};
```

#### Что показывает статус:

- ✅ **Онлайн**: отображает количество игроков (например, "Онлайн: 80/100 игроков")
- ❌ **Оффлайн**: когда сервер недоступен
- 🔄 **Автообновление**: каждые 30 секунд
- 👆 **Ручное обновление**: клик по статусу

### Настройка счетчика

Счетчик работает автоматически через localStorage:
- Считает уникальных посетителей (один браузер = один посетитель)
- Данные хранятся локально в браузере пользователя
- Для сброса: очистите localStorage или удалите ключи `visitCount` и `hasVisited`

## 📁 Структура проекта

```
PEI-site/
├── index.html              # Главная страница
├── css/
│   └── style.css          # Стили с CSS переменными для тем
├── js/
│   ├── particles.js       # Анимация частиц на фоне
│   ├── parallax.js        # Параллакс эффекты
│   ├── counter.js         # Счетчик посещений
│   ├── server-status.js   # Live статус сервера (BattleMetrics API)
│   └── theme.js           # Переключение темной/светлой темы
└── README.md              # Документация проекта
```

## 🎨 Кастомизация

### Изменение цветов темы

Отредактируйте CSS переменные в `css/style.css`:

```css
:root {
    --bg-primary: #0a0a0a;
    --text-primary: #fff;
    /* ... другие переменные */
}

body.light-theme {
    --bg-primary: #f3f4f6;
    --text-primary: #1f2937;
    /* ... другие переменные */
}
```

### Настройка частиц

В `js/particles.js` измените параметры:
```javascript
this.particleCount = 80;        // Количество частиц
this.mouse.radius = 150;        // Радиус взаимодействия с мышью
```


## 🌐 Деплой

### GitHub Pages
1. Загрузите все файлы в репозиторий
2. Включите GitHub Pages в настройках
3. Сайт будет доступен по адресу `https://username.github.io/repo-name/`

### Обычный хостинг
Просто загрузите все файлы на хостинг - сайт статический и не требует серверной части.

## 🔧 Технологии

- HTML5 Canvas для частиц и игры
- CSS3 Variables для тем
- Vanilla JavaScript (без фреймворков)
- localStorage для хранения данных
- Адаптивный дизайн

## 📝 Лицензия

Проект создан для COBRA PEI #1

## 👤 Контакты

- Telegram: @dybeky
- Discord: dybeky
- GitHub: [custosAC](https://github.com/dybeky/custosAC)
